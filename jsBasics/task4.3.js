const radius = 5;
const height = 10;
const volume = Math.PI * (radius ** 2) * height;
const roundedVolume = volume.toFixed(2);

console.log("Radius:", radius);
console.log("Height:", height);
console.log("Volume of cylinder:", roundedVolume);
